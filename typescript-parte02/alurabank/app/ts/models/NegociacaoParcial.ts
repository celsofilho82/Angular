export interface NegociacaoParcial{

    // Criamos uma interface para encapsular os dados que recebemos da API blindando
    // qualquer mudança inesperada da API.

    vezes: number;
    montante: number;

}